from kivy.app import App
from kivy.uix.label import Label

class TestApp(App):
    def build(self):
        # 这是一个极简版，确保打包流程能100%跑通
        return Label(text='Hello! Private Player Success!')

if __name__ == '__main__':
    TestApp().run()
